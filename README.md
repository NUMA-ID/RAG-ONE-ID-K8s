# RAG ONE ID — Infra Kubernetes (PROD)

Dépôt local (pas de remote) contenant les manifests Kubernetes de
production pour le projet RAG ONE ID, sur le modèle du dépôt
`appel-offre-k8s` (AO-ID).

**État actuel : vide.** Aucun manifest n'a été écrit — le cluster et
le namespace cibles pour ce projet n'ont pas encore été confirmés par
l'utilisateur (voir `docs/BLOCAGES.md` du dépôt PREPROD,
`/home/numa/projets/RAG/PREPROD/docs/BLOCAGES.md`).

Procédure de bascule : voir le skill `prod_k8s` (actuellement rédigé
pour AO-ID — à adapter/dupliquer pour RAG une fois le cluster cible
confirmé).
